{
  "name": "destura-template-home-services",
  "private": true,
  "type": "module",
  "description": "Destura template library — Home Services (HVAC/Plumbing/Electrical). No dependencies: this is a config flag only, so scripts/apply-config.js and api/chat.js can use import/export syntax. Zero build step for the deployed site itself.",
  "scripts": {
    "apply-config": "node scripts/apply-config.js"
  }
}
